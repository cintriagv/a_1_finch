attempt_0_app_local:

JAVASCRIPT ATTEMPT:

DEPENDENCIES:

node.js: https://nodejs.org/
express.js: https://expressjs.com/
node-fetch: https://www.npmjs.com/package/node-fetch

HOW TO RUN:

>ensure node.js/express.js/npm fetch are installed

>download files from repository
>start server by navigating to the respective directory in terminal and run node index.js
>navigate to https://cxiv.io/finch_tech_assessment_begin.html
>click finch logo to start authentication

>load index
> user click on ondex calls local host http for redirect to authorization
> redirected to redirect URI with code
>save code to be sent back to server
>server calls finch for token
>now we can start making calls to Finch's API


